<UserControl x:Class="Hat.Views.Controls.ChatArea"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Hat.Views.Controls"
             xmlns:converters="clr-namespace:Hat.Converters"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Messages area -->
        <ScrollViewer Grid.Row="0" x:Name="ChatScrollViewer"
                      VerticalScrollBarVisibility="Auto" Padding="16,12">
            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel MaxWidth="680" HorizontalAlignment="Center" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:ChatBubble Margin="0,3" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty state -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="{Binding Messages.Count, Converter={StaticResource BoolToVis}, ConverterParameter=empty}">
            <Border Width="56" Height="56" CornerRadius="28"
                    Background="{DynamicResource SurfaceTertiaryBrush}"
                    HorizontalAlignment="Center" Margin="0,0,0,16">
                <TextBlock Text="🎩" FontSize="24"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <TextBlock Text="Hat" FontSize="20" FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       HorizontalAlignment="Center" />
            <TextBlock Text="Pergunte qualquer coisa..." FontSize="12"
                       Foreground="{DynamicResource TextMutedBrush}"
                       HorizontalAlignment="Center" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Input area -->
        <Border Grid.Row="1" Padding="16,8,16,16"
                Background="{DynamicResource BackgroundBrush}">
            <Grid MaxWidth="680" HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" x:Name="ChatInput"
                         Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="False" TextWrapping="Wrap"
                         MaxHeight="120"
                         FontSize="13"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         Background="{DynamicResource InputBackgroundBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         BorderThickness="0.5"
                         Padding="14,10"
                         KeyDown="ChatInput_KeyDown"
                         AllowDrop="True" PreviewDragOver="ChatInput_DragOver"
                         Drop="ChatInput_Drop"
                         Style="{StaticResource GlassTextBox}" />

                <Button Grid.Column="1" Width="36" Height="36" Margin="10,0,0,0"
                        Click="Send_Click"
                        Style="{StaticResource AccentIconButton}">
                    <TextBlock Text="&#xE724;" FontFamily="Segoe MDL2 Assets" FontSize="16" />
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
